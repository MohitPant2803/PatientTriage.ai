PORT=5000
NODE_ENV=development
# Optional: Google Gemini API Key for real-time generative clinical reasoning & NLP
# If omitted or left empty, the system automatically uses deterministic offline clinical intelligence!
GEMINI_API_KEY=
# Optional: MongoDB connection string. If omitted, uses fast in-memory store with auto-seeding!
MONGODB_URI=mongodb://localhost:27017/patienttriage
